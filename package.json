{
  "name": "workers-cache-r2-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Cache a Cloudflare Worker's output with Workers Cache, backed by R2. The worker runs only on a cache miss; one canonical key drives both the cache and the R2 object.",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^4.69.0"
  }
}
